{
"name": "truncate-escaped-html",
"main": "truncate-escaped-html.js",
"version": "0.0.1",
"description": "Converts special characters in a string to the corresponding HTML entities, then truncates the resulting string carefully (HTML entities are not cut in the middle).",
"keywords": ["HTML", "escape", "entities", "entity", "truncate", "truncated", "shorten", "shortened"],
"author": { "name": "Mithgol the Webmaster" },
"license": "MIT",
"repository": {
   "type": "git",
   "url": "https://github.com/Mithgol/node-truncate-escaped-html.git"
},
"scripts": {
   "test": "jshint --verbose truncate-escaped-html.js"
}
}